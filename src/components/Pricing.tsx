import React from "react";
import { ExternalLink, ShieldCheck, Sparkles } from "lucide-react";
import { MOCK_PLANS } from "../data/mockData";
const guidance: Record<string, string> = { mensal: "Para testar sem compromisso.", trimestral: "Melhor para atravessar o próximo ciclo de provas com mais previsibilidade.", semestral: "Para quem quer deixar o semestre mais organizado de uma vez." };
const ctas: Record<string, string> = { mensal: "Começar mensal", trimestral: "Escolher trimestral", semestral: "Escolher semestral" };
export const Pricing: React.FC = () => <section id="planos" className="mx-auto max-w-7xl scroll-mt-24 border-t border-[var(--line)] px-4 py-16 sm:px-6 sm:py-24 lg:px-8"><div className="mx-auto mb-12 max-w-3xl text-center sm:mb-14"><div className="mb-4 inline-flex items-center gap-2 rounded-[var(--radius-pill)] bg-[var(--yellow)] px-3.5 py-1.5 text-xs font-extrabold tracking-wider text-[var(--ink)]"><Sparkles className="h-3.5 w-3.5" aria-hidden="true" /> PREÇO ACESSÍVEL NO PRIMEIRO CICLO</div><h2 className="text-balance text-3xl font-extrabold tracking-tight text-[var(--ink)] sm:text-5xl">Escolha o plano e estude com menos caos.</h2><p className="mt-4 text-base font-medium text-[var(--ink-soft)] sm:text-lg">Todos os planos incluem acesso aos materiais disponíveis na plataforma. Comece pela amostra grátis e entre se fizer sentido para sua rotina.</p></div><div className="grid items-stretch gap-6 md:grid-cols-3">{MOCK_PLANS.map((plan) => { const equivalent = (plan.standardPrice / plan.intervalMonths).toLocaleString("pt-BR", { minimumFractionDigits: 2 }); return <article key={plan.id} className={`relative flex flex-col rounded-[var(--radius-card)] border bg-[var(--paper)] p-6 ${plan.popular ? "border-2 border-[var(--ink)] shadow-md" : "border-[var(--line)]"}`}><div className="min-h-8">{plan.popular && <span className="inline-flex rounded-[var(--radius-pill)] bg-[var(--yellow)] px-3 py-1 text-xs font-extrabold tracking-wide text-[var(--ink)]">RECOMENDADO</span>}</div><h3 className="mt-3 text-2xl font-extrabold tracking-tight text-[var(--ink)]">{plan.name}</h3><p className="mt-2 min-h-10 text-sm font-medium leading-relaxed text-[var(--ink-soft)]">{guidance[plan.id]}</p><div className="mt-6 border-y border-[var(--line)] py-5"><p className="text-xs font-extrabold uppercase tracking-wider text-[var(--muted)]">Hoje</p><p className="mt-1 text-4xl font-extrabold tracking-tight text-[var(--ink)]"><span className="mr-1 text-xl">R$</span>{plan.firstMonthPrice}</p><p className="mt-1 text-xs font-medium text-[var(--muted)]">Primeiro ciclo · de R$ {plan.standardPrice}</p></div><div className="mt-5 space-y-1.5 text-sm"><p className="font-extrabold text-[var(--ink)]">Depois: R$ {plan.standardPrice} a cada {plan.intervalMonths === 1 ? "mês" : `${plan.intervalMonths} meses`}</p><p className="font-medium text-[var(--ink-soft)]">Equivale a R$ {equivalent}/mês</p></div><a href={plan.checkoutUrl} target="_blank" rel="noopener noreferrer" className={`mt-7 flex min-h-12 w-full items-center justify-center gap-2 rounded-[var(--radius-lg)] px-6 py-3.5 text-sm font-extrabold shadow-2xs transition-opacity hover:opacity-90 ${plan.popular ? "bg-[var(--ink)] text-[var(--paper)]" : "bg-[var(--yellow)] text-[var(--ink)]"}`}>{ctas[plan.id]} <ExternalLink className="h-4 w-4" aria-hidden="true" /></a></article>})}</div><div className="mt-8 border-t border-[var(--line)] pt-8"><div className="flex items-start gap-3"><span className="flex h-10 w-10 shrink-0 items-center justify-center rounded-[var(--radius-icon)] bg-[var(--success-surface)] text-[var(--success-ink)]"><ShieldCheck className="h-5 w-5" aria-hidden="true" /></span><p className="text-sm font-medium leading-relaxed text-[var(--ink-soft)]"><span className="font-extrabold text-[var(--ink)]">7 dias de garantia.</span> Acesse, teste e peça reembolso pela Kiwify se não fizer sentido para sua rotina.</p></div></div></section>;
